#!/bin/sh

set -e

cd "$(dirname "$0")/.."

gem install bundler -v '~> 2.0'
bundle install
bundle exec ruby exe/spandx-rubygems update
git add lib
git status
git commit -m 'Update index'
git push origin
